<div class="sortable_items attachments" data-url="/backends/attachments/sort">
	<% @attachments.each do |attachment| %>
	<% @attachment = attachment %>
	<%= render partial: "/backends/attachments/form" %>
	<% end %>
</div>
<div class="form-group">
	<div class="col-lg-3"></div>
	<div class="col-lg-9">
		<button type="button" class="btn btn-success new_attachment">
			NEW ATTACHMENT
		</button>
	</div>
</div>
